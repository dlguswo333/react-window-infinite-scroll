{
  "name": "react-window-infinite-scroll-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "scripts": {
    "test": "npm test -w package",
    "build": "npm run build -w package -w preview",
    "lint": "eslint .",
    "start": "concurrently \"npm run watch -w package\" \"npm start -w preview\"",
    "deploy": "npm publish -w package"
  },
  "workspaces": [
    "preview",
    "package"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "concurrently": "^8.2.1",
    "eslint": "^8.50.0",
    "eslint-plugin-react-hooks": "^4.6.0"
  }
}
